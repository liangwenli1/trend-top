# Trend Top 产品与竞品分析

- 日期：2026-09-16
- 代码：https://github.com/liangwenli1/trend-top @ `800bb4d`
- 视角：用户 + 产品经理
- 目的：看清和竞品的差距，以及 Firecrawl 配上之后真正该干什么

---

## 1. 产品一句话

Trend Top 想当开源发现层：Skill / 插件 / Agent / 组件 / 网站 / 仓库六类，提供 Trending、Ranking、Charts、官方、同类对比，再加上账号、Creem 付费和每日邮件。

定位对。执行上仍偏「GitHub 仓库排行榜 + 六种标签」。用户要解决的是「这一类里该用哪个」。竞品在各自垂直里已经用安装量、质量分、可试用回答了这个问题。

---

## 2. 用户打开网站会怎么想

### 会留下来的点

- 六类入口比 GitHub Trending 清楚：不是「今天又火了什么」，而是「我要 skill / MCP / 组件」。
- 热度公式公开（Star 增量、增速、Fork、push 新近度），比「纯看总 Star」诚实。
- 官方标记、同类 / 重复、对比 2–3 个，方向对：把选择权留给用户。
- 中英双语；邮件里有涨跌箭头和 PNG 曲线，适合每天扫一眼。
- 登录、Google、退款、后台候选，已经像能卖钱的站，不像 demo。

### 会关掉的点

1. **决策信号还是 Star。** Skills.sh 用安装量，Glama 用质量分和 npm 下载，Smithery 用调用次数。Trend Top 的 skill / 插件大多还是 GitHub Star，用户分不清「好用」还是「仓库红了」。
2. **装不上、试不了。** Skills.sh 是 `npx skills add`，Smithery 是一键托管 + OAuth。站内有 `install` 字段，但没有统一安装 / 试用路径，发现停在「看一眼仓库」。
3. **Website 名不副实。** 独立站点注册表只有 6 个源；排名仍大量用关联仓库 Star。21st.dev / skills.sh / Glama 自己就是目录，目前只抓了首页 title / description，没有把他们的榜单变成 Trend Top 的数据。
4. **Skill 变严之后会空。** 现在必须有 `SKILL.md` 才算 skill，假阳性少了，但 skills.sh 那种安装量很高的目录盖不住。用户进 Skills 容易觉得「就这几条？」
5. **邮件被改成 Pro。** 原先免费价值就是每日摘要。现在没付费就不发。免费用户只剩浏览，而浏览又不如垂直站深，转化会很难。
6. **「全网热榜」预期对不上。** 上限约 2000 仓、每类 400 条。首页数字好看，但不是 GitHub 全集，漏收仍会发生。

---

## 3. 产品经理视角

| 问题 | 现状 | 风险 |
| --- | --- | --- |
| 付费点 | 日更邮件锁 Pro；`PRODUCT.md` 仍写摘要免费 | 文档和产品打架；免费钩子被抽掉 |
| 差异化 | 六类 + 方法论文档 | 每类都被更专业的站打穿 |
| 数据护城河 | GitHub Search + 少量官网 meta | 没有安装 / 调用 / 质量一等数据 |
| 转化漏斗 | 看榜 → 注册 → 订邮件 → 付费 | 看榜阶段已被竞品满足 |
| 覆盖 vs 纯度 | 刚收紧分类，避免仓库复制成 skill | 纯度升、供给降，首页六类会更空 |
| Pro 权益 | 几乎只有邮件 | 用户会问「付费多了什么图表 / 对比 / 告警？」 |

该赢的缝是 **跨类型的「同类里怎么选」**，不是再做一个 GitHub Trending。图表、对比、官方、cluster 都有雏形，但缺少安装量、质量、可运行性这三块硬数据，付费也还没接到这些能力上。

---

## 4. 竞品

至少看四家，覆盖 Skill、插件、仓库热度、可运行插件。

### 4.1 Skills.sh — Skill 目录

- 地址：https://skills.sh
- 只做 Agent Skill，排行靠 **安装量和 8 周活跃**，一条命令安装。
- 弱：几乎没有官方标、分类、图表、中文、邮件。
- 对 Trend Top：Skill 页如果还是 Star 榜，用户会直接去 skills.sh。必须吃到安装量 / 兼容哪些 agent，或明确承认「我们是跨类型，Skill 深度不拼安装量」。

### 4.2 Glama MCP — 插件 / MCP

- 地址：https://glama.ai/mcp/servers
- **8.8 万+** 服务器，质量 A–F、官方、工具数、许可证、维护状态、npm 下载。
- 弱：只做 MCP，噪音大。
- 对 Trend Top：插件类如果没有质量分，官方只靠 org 名白名单，专业用户会觉得不可信。

### 4.3 GitHub Trending 系 — 仓库热度

- GitHub 官方 Trending 是心智入口。
- whatstrending.ai、findarepo 已经在算 **二阶增速（加速 / 变冷）**，不只是 7 日增量。
- 弱：不分 skill / 插件 / 组件，没有邮件订阅产品化。
- 对 Trend Top：仓库榜公式已经不错，但缺「这周在加速还是在熄火」。用户看热度会去这些站；看「该用哪个 PDF skill」才可能留在这里。

### 4.4 Smithery — 插件运行时（对照）

- 地址：https://smithery.ai
- 2 万+ MCP，**用量**、托管、OAuth、CLI。发现即能连。
- 对 Trend Top：发现如果不接到「能用」，只是另一个列表。

**21st.dev / shadcn** 不是排行站，是组件注册表和文档。组件类用户要的是「复制即用」，不是 Star 曲线。Trend Top 替代不了它们，只能告诉用户「同类组件里先看哪几个」。

### 对照表

| | Trend Top | Skills.sh | Glama | GitHub Trending 系 |
| --- | --- | --- | --- | --- |
| 覆盖 | 六类，浅 | 只 skill，深 | 只 MCP，极深 | 只仓库 |
| 核心指标 | Star 增量 | 安装量 | 质量分 + 下载 | 短窗 Star / 加速度 |
| 装上就能用 | 弱 | 强 | 中 | 无 |
| 官方 / 去重 | 有，偏启发式 | 弱 | 强 | 无 |
| 邮件 / 账号 / 付费 | 有 | 无 | 弱 | 无 |
| 中文 | 有 | 无 | 弱 | 无 |

能赢的缝：**跨类型 + 同类对比 + 官方 / 重复说明 + 中文日更**。缝在，深度不够。

---

## 5. 还做得不好（按该先改的顺序）

1. **Pro 卖的不是「更好的发现」。** 锁邮件会伤免费循环。更合理：免费日更摘要（缩短 / 延迟），Pro = 自定义多类型、对比导出、告警、完整历史、质量信号。
2. **Website 不是网站目录。** 6 个源只刮首页。独立指标未做完：内容更新、目录新增、讨论、关联仓动量；Star 只能当一个信号。
3. **垂直数据没接进来。** 不爬 skills.sh 安装量、不吸收 Glama 质量分，六类就是六个 GitHub 搜索。
4. **Skill / 插件供给与可信度两头不靠。** 太松会脏，太严会空。需要：`SKILL.md` 验证 + 安装量 / 质量分，而不是二选一。
5. **「最好用的几个」还不够产品化。** 分类页 2–4 个推荐有了，但依据仍是 Star / 手工 note，没有「官方 / 维护中 / 安装最多 / 最近在涨」并排。
6. **对比太像仓库对比。** 至少插件应对安装命令、协议、质量分；更完整的对比还会拉下载、包体、CVE。
7. **没有试用 / 安装主路径。** 详情页 CTA 应是「安装这条 skill / 加这个 MCP」，GitHub 是第二按钮。
8. **文档过期。** `PRODUCT.md` 写摘要免费、Pro 功能未定；代码已 `PRO_REQUIRED`。对外说辞会裂。
9. **样本宇宙要讲清楚。** 「最近在涨」可以，不要让人以为是全网官方热榜。漏收会伤信任。
10. **运营闭环不完整。** 异常增益「待审核」没有审核台；候选队列有了，没有「为什么没进榜」的公开解释。

---

## 6. Firecrawl 配了能干什么

后台「Website 数据采集」里填 Firecrawl API Key 之后：

### 现在实际在用

只服务 Website 来源注册表（21st.dev、Skills.sh、Smithery、Glama、Agent Skills、shadcn/ui）：

- 调用 Firecrawl `v2/scrape`（markdown + links，只要正文）
- 比直接 fetch 强在：能渲染 JS 页，稳定拿到 title / 描述 / favicon / RSS / sitemap / 更新时间
- 失败会按 robots、限速、指数退避
- 没配 Key 时走受限的直接 HTML 元数据读取

一句话：**现在 Firecrawl = 让 6 个官网的标题描述更准。**

### 配了但还没做、对产品最有用的

| 能力 | 用来干什么 |
| --- | --- |
| Scrape + JSON schema | 把 skills.sh 排行、Glama 质量分、Smithery 用量抽成结构化字段，写进 `ranking_signals` |
| Crawl | 把目录站整站列表变成 Website / Skill / 插件条目，而不只是 6 个首页 |
| Map | 发现 `/skills`、`/servers` 等列表 URL，自动扩源 |
| Search | 找新的 skill / MCP 目录，补 GitHub Search 盲区 |
| Monitor | 目录或文档变了再触发采集，而不是每天盲打 |
| Agent / Extract | 「把这个域名里所有带安装命令的条目抽出来」 |

真正该用它做的是：**把竞品目录变成你们的数据源**（安装量、质量分、列表页）。这样 Website / Skill / 插件才不是「又一个 GitHub 搜索」。

建议先对 skills.sh 和 Glama 各做一条结构化 scrape，把安装量 / 质量分显示在对应详情和榜单上。这比再把仓库上限从 2000 提到 5000 更能拉开差距。

---

## 7. 建议的下一步（产品，不是工程堆砌）

1. 先定付费边界：免费保留发现 + 短摘要，Pro 卖信号和告警，不要只锁邮件。
2. 同步改 `PRODUCT.md`，避免对外还说摘要免费。
3. Firecrawl 从「刮 6 个首页」升级到「抽 2 个垂直目录的硬指标」。
4. 详情页把安装 / 试用做成主按钮。
5. 分类推荐从「2–4 个 Star 高的」改成「官方 / 最常用 / 涨得快」并排，仍把选择权交给用户。

---

*本报告基于 2026-09-16 对仓库 `800bb4d` 的代码阅读，以及对 Skills.sh、Glama、Smithery、GitHub Trending / whatstrending.ai 的公开页面核对。竞品数据会变，结论看能力结构，不看当天名次。*
