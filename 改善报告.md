# Trend Top 改善报告

日期：2026-09-14  
范围：产品定位、信息架构、竞品差距、数据模型与分阶段落地  
本文取代「只做更好的 GitHub 热榜 + 日报」这条路线，作为下一阶段改站依据。现有 [PRODUCT.md](./PRODUCT.md) 描述的是 v0.2 已上线能力；与本文冲突时，以本文为准。

---

## 1. 结论（先看这段）

站要扩大，不只跟踪 GitHub 仓库。第一层按 **资产类型** 切开：skill、plugin（含 MCP）、components、website、github-repo。每类都提供 **Trending、排行、Charts**，并补上种类页、详情、对比、同类去重、官方标记、搜索和订阅。

推荐可以写，而且应该写。约束只有一条：**推荐的是一组选项（默认 2～4 个），写清差异，由用户选。** 不做「这一类只用这一个」。

付费价值不是又一份热榜邮件，而是：同类太多时帮用户收束噪音，把官方、重复、名次变动和对照曲线送到他盯的类型/种类上。

---

## 2. 现状

v0.2 已具备：

- 双语站点：`/{locale}/home`、`/ranking`、`/charts`
- 透明热度分（Star 增量、增速、Fork、push 新鲜度）
- 日/周/月窗口、筛选、分页、Recharts
- 验证后的每日邮件摘要
- Postgres / PGlite，日指标与周期预聚合

这套能力对「仓库热榜」成立，但对用户真正的使用场景不够：开发者要找的是能装的 skill、插件、MCP、组件、开源网站。这些东西每天都在涨、重复极多，仓库粒度的一张总榜回答不了「这类里前几名是谁、哪个是官方、哪个是换皮」。

---

## 3. 真正的痛点

用户不是找不到。目录和 GitHub 上已经能看见。痛点是：

1. **太多** — 同一需求下几十上百条，刷不完。
2. **重复** — fork、换皮、同名 skill / 同 tool 的 MCP。公开研究里，skill 市场近一半条目与其他条目重名。
3. **分不清官方** — 厂商源、官方 registry、社区仿写混在一起。
4. **不知道这类里该看哪几个** — 不是需要系统替他锁死一个，而是需要前几名 + 差异，自己选。
5. **迭代太快** — 昨天的首选今天可能被超车，需要推送名次和版本变动，而不是再做一个静态目录。

「找不到」是目录的问题。我们要做的是 **时间轴上的名次、官方与重复的标记、以及一组带说明的选项**。

---

## 4. 竞品与差距

调研对象不少于三家，覆盖仓库热榜、MCP 目录、skill 市场、组件 registry。数字随目录变化，下表取 2026 年量级，用于定位而不是精确对账。

| 竞品 | 做什么 | 规模量级 | 对用户痛点 | 缺口 |
|---|---|---|---|---|
| GitHub Trending | 仓库日/周/月热度 | 官方入口 | 习惯、流量 | 无 skill/插件粒度；无官方 vs 换皮；无种类内对照 |
| OSS Insight / Star History | 事件分析、Star 曲线 | 全 GitHub 量级 | 深度数据 | 分析工具，不是「这类前几名该怎么选」 |
| HelloGitHub | 月刊编辑精选 | 仓库级 | 中文信任 | 月更，覆盖不到日更的 agent 栈 |
| Glama / mcp.so / Smithery | MCP 目录 + 安装 | 数万～十万+ server | 搜得到、能装 | 条数竞赛；同类并列；官方要自己辨 |
| PulseMCP | 人工过的 MCP 目录 | 相对少 | 质量过滤 | 仍是目录，不是按类型的排行/图/推送 |
| MCP Toplist | 全网 MCP 综合分排行 | ~12.8 万 | 有榜、有曲线 | 全球第一永远是头部项目；回答不了「Postgres MCP 前三名」 |
| Claude Market / ClaudSkills 等 | skill 搜索与安装量 | 数千到数万 | 能搜 | 去重弱；fork 并存；官方集极窄（Anthropic 官方约数十个） |
| 21st / Magic UI / Aceternity | 组件市场与 registry | 按库 | 预览、安装 | 同一组件多个源，没有跨源排行和官方对照 |
| 官方 MCP Registry / anthropics/skills / shadcn 官方源 | 权威源 | 很小 | 真官方 | 覆盖窄，不是发现层 |

共同结构：要么 **全量目录**（搜得到、噪音大），要么 **全站热榜**（第一名永远是巨头），要么 **人工月刊**（慢）。没有产品同时做到：

- 按资产类型切开（skill / plugin / components / website / repo）
- 每类里有 Trending、排行、Charts
- 种类内给前 N 名，并写一组推荐选项
- 官方、重复作为事实列出来
- 订阅推的是名次变动，不是又一份目录

这是本站可以占住的位置。不要和 PulseMCP 抢「Pulse = MCP 目录」的心智；对外名称用 **Trend Top**，类型里 plugin 包含 MCP 即可。

---

## 5. 产品原则

1. **类型是第一层。** 全站不再以「一个 GitHub 仓库热榜」为唯一首页。
2. **三件套保留：** Trending、排行、Charts。这是主产品，不是周刊评论。
3. **给前几名，不给唯一答案。** 排行默认 Top 20，可翻页。推荐位默认 2～4 个选项。
4. **推荐是判断，不是锁死。** 写差异（官方 / 更勤 / 更轻 / 安装量），用户可以去完整榜和对比页不同意。
5. **官方和重复是事实，不是结论。** Official badge、同类 N 条，详情写判定依据。
6. **热度只在同类里比。** skill 不和 next.js 抢同一条榜。
7. **GitHub 仓库热榜保留**，降为五个类型之一，不删。

---

## 6. 信息架构

### 6.1 五个类型

| `type` | 排什么 | 发现单元 |
|---|---|---|
| `skill` | Agent skill | 一条 `SKILL.md`，不是整个巨仓 |
| `plugin` | 插件、MCP server | 清单 / `server.json` / marketplace 条目 |
| `components` | UI 组件、registry 项 | shadcn registry item 或等价单元 |
| `website` | 开源网站、在线工具 | 可访问的站点 + 其源码仓 |
| `github-repo` | 仓库 | 现有 Pulse 的仓库粒度 |

同一仓库可以在多类出现：一个 repo 里多个 skill 就是多条。

### 6.2 页面路由

语言前缀保持 `/{locale}`。类型之下：

```
/{locale}/{type}                      Trending（该类默认页）
/{locale}/{type}/ranking              排行
/{locale}/{type}/charts               Charts
/{locale}/{type}/c/:category          种类：推荐一组 + 完整榜 + 对照图
/{locale}/{type}/compare              对比（?ids=）
/{locale}/{type}/official             官方列表（也可用 ranking 的 official 筛）
/{locale}/{type}/:id                  详情（含 similar、短推荐）
/{locale}/search                      跨类型或带 type 参数的搜索
/{locale}/home                        五类入口 + 各类 Trending 摘要（取代「只有仓库图」的首页）
```

`filters` 不单独成页，挂在 ranking / 种类 / official 上。

现有 `/ranking`、`/charts` 视为 `github-repo` 的旧入口，改版时重定向到 `/github-repo/ranking`、`/github-repo/charts`。

### 6.3 HTTP API

不要再让一个大 `/api/rankings` 混装所有东西。

```
GET /api/{type}/trending
GET /api/{type}/rankings
GET /api/{type}/charts
GET /api/{type}/filters
GET /api/{type}/categories
GET /api/{type}/categories/:slug
GET /api/{type}/compare?ids=
GET /api/{type}/items/:id
GET /api/{type}/items/:id/similar
GET /api/search?type=&q=
```

查询参数沿用窗口（1/7/30 日）、语言、分页；各类型可有自己的筛（客户端、registry、官方与否）。

---

## 7. 各页做什么

### Trending `/{type}`

短窗口（24h / 7d）。只回答现在谁在动：新进前 N、增速、名次变化。不是「今天又上架了 200 个」。

### 排行 `/{type}/ranking`

日 / 周 / 月稳定表。列（没有就标数据不足，不编造）：

- 名次、名称、窗口增量、热度分
- Official（属性）
- 同类重复数 / fork 数
- 上次提交、许可证
- 入口：详情、加入对比、同类

第一名只是分最高，不是「请用这个」。

### Charts `/{type}/charts`

前 N 对照曲线、类型/语言构成、新秀 vs 老项目年龄。图给选择用，不宣布冠军。

### 种类 `/{type}/c/:category`

「太多」的真正入口。例如 `/plugin/c/postgres`、`/skill/c/pdf`。固定三块：

1. **推荐 2～4 个** — 各写一句适合谁、和另外几个的差别
2. **该种类完整排行**
3. **对照图**

### 详情 `/{type}/:id`

安装/仓库/站点、是否官方及依据、本条曲线、同类 N 条、与相邻名次的短推荐。列表没有详情就留不住人。

### 对比 `/{type}/compare`

从排行勾 2～3 条。曲线、官方、活跃、重复、许可同一张表。这是把选择权交给用户的页。

### 官方 `/{type}/official`

仅官方源。判定见第 8 节。可与 ranking 的 filter 合并，若独立成页是为了「我只想看官方」这一跳。

### 搜索

类型多了之后筛不够用。先类型内搜，再全站搜。

---

## 8. 官方、重复、推荐、打分

### 官方（自动打标，详情展示依据）

- **MCP / plugin：** 在 Official MCP Registry，或 org/包名对得上厂商（如 `microsoft/playwright-mcp`、`supabase`）。
- **Skill：** `anthropics/skills` 或已验证 org 的 marketplace。
- **Components：** shadcn 官方 registry，或作者自己的源仓（不是第三方市场里的一份拷贝）。
- **Website / repo：** 与产品同名的厂商 org、GitHub Verified、官方文档指向的仓。

未命中就不标，不猜。

### 同类聚类（去重，不删数据）

主榜默认折叠，详情和 similar 展开。规则：

- 名称归一化（大小写、符号）
- GitHub fork / 同源
- MCP 暴露的 tool 名相近
- SKILL.md 用途相近
- 组件在不同 registry 的同一 slug

主榜一行可显示「+12 similar」，不把 12 个换皮全部铺开。

### 推荐（一组选项）

从该种类排行抽出 2～4 个，允许写推荐语，例如：

- A：官方，适合生产
- B：社区、更新更勤，适合先试用
- C：更轻，适合只需要读操作

不写「请只装 A」。用户可忽略推荐，看完整榜或去对比页。

### 热度分

沿用 v0.2 思路（窗口增量、增速、新鲜度；有 Fork/安装则纳入），但：

- **分母是该 type（再按种类）的候选集**，不做跨类型混排
- 安装量、release、registry 出现次数，有则用，没有就显示数据不足
- 异常增量（相对上期过高）继续剔除待审，防止刷榜

---

## 9. 数据模型（相对 v0.2 的扩大）

现在是 `repos` + `daily_metrics` + `period_metrics`。以后：

```
category                         种类（type + slug，如 plugin/postgres）
  └── assets                     skill / plugin / component / website / repo
        ├── source_repo          可空；website 另有 canonical_url
        ├── official             布尔 + evidence
        ├── cluster_id           同类聚类
        └── daily_metrics        日指标打在 asset 上，不打在巨仓上
period_metrics                   按 asset + 窗口预聚合，SQL 出榜
```

`github-repo` 可继续直接用 `repos`，避免一次改崩现有榜。采集从「只搜热门仓」扩大为：

- 官方 MCP Registry、Glama/Smithery 等可引用索引（去重后入库，不依赖单一目录）
- GitHub 上的 `SKILL.md`、`marketplace.json`、`plugin.json`
- shadcn `registry.json` 与官方源
- 开源网站：有源码仓且可访问的产品站（规则另行收紧，避免把任意 landing 扫进来）

候选集仍然有上限（每类型每轮 bounded），不做全网 12 万 MCP 的镜子。价值在种类内名次和去重，不在条数。

---

## 10. 订阅与变现

免费：

- 各类型 Trending / 排行 / Charts 的公开层（可延迟或 Top 较浅）
- 种类页的推荐组可见（可限次数或限种类数）

订阅推送（仍是榜 + 图 + 短推荐，不是长文周刊）：

- 用户勾选 **类型 + 窗口**（如 skill 周榜、plugin 日榜）
- 可再盯 **种类或单条**
- 触发：前 N 变动、官方新发、挑战者窗口增速超过当前第一、同类重复突然变多、关注条目发新版本

邮件结构建议：该类/该种类 **Top N 表 + 一张对照图 + 推荐 2～4 个的差异各一行**。用户在这组里选。

付的是「少看 99 个重复、第一眼看到值得考虑的几个」，不是又一份 GitHub 热榜。

---

## 11. 分阶段

### 第一期（改站骨架，必须做）

- 路由与 API 按 `{type}` 切开；旧 `/ranking`、`/charts` 重定向到 `github-repo`
- 首页改为五类入口
- `github-repo` 迁入现有榜、图、邮件，行为不回退
- 上 `skill` + `plugin` 两条新类型（先 bounded 采集 + 演示数据）
- 详情页、种类页（推荐组 + 榜 + 图）

没有种类，推荐没处放；没有详情，对比和同类也没处放。

### 第二期（对准痛点）

- 聚类 / similar、Official 打标与依据
- 对比页
- 搜索
- 订阅细到种类和单条（名次/版本变动）

### 第三期（补全类型与信号）

- `components`、`website`
- 安装量、release 等非 Star 信号（有则用）
- 维护/停更警告作为列，不单独做成说教页

---

## 12. 明确不做

- 不做 12 万条 MCP / skill 的全量镜子，不和 Glama 比条数
- 不把主产品做成周刊评论或「今日只推荐一个」
- 不替用户锁死唯一选择
- 不编造缺失指标
- 不把 skill、插件、组件和 next.js 放进同一张热度榜
- 第一期不做账号体系（订阅继续用验证邮箱即可，与现网一致）

---

## 13. 成功标准（改版后怎么算做对）

- 用户能在五个类型间切换，每类都有 Trending / 排行 / Charts
- 打开一个种类页，能看到 **2～4 个带差异的推荐** 和完整前 N，而不是一个「首选」
- 重复项在主榜折叠，能从详情进入同类列表
- Official 有依据，未命中不标
- 点排行能进详情，能勾选进对比
- `github-repo` 的日/周/月榜与现有方法论一致，旧 URL 可跳转
- 订阅邮件是该类型/种类的 Top N + 图 + 推荐组，不是长文

---

## 14. 与现有文档的关系

| 文档 | 角色 |
|---|---|
| [README.md](./README.md) | 如何运行、直播数据、现热度公式 |
| [PRODUCT.md](./PRODUCT.md) | v0.2 已实现的产品决策与验收 |
| [DESIGN.md](./DESIGN.md) | 视觉与页面节奏；改版时类型导航替换「Home / Rankings / Charts」单层结构，原则不变 |
| **本报告** | v0.3 方向：类型第一层、推荐一组选项、种类/详情/对比/官方/去重 |

落地时先改 PRODUCT 验收条款，再改路由与 schema，避免文档与实现各写一套。
