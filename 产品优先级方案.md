# Trend Top 改动清单（产品主拍板）

日期：2026-09-16

主场是开源资源，**大部分在 GitHub**，独立站是少数目录/官网。排行只关心：**热度 + Star + 用的人多不多**。不管页面更不更新。详情只给 GitHub 或官网，不给安装/复制命令。Free 只能逛站；每日摘要按定价页，是 Pro。

---

## 现在就改

### 文档 / 付费

1. `PRODUCT.md`、`README` 跟定价页对齐：Free = 浏览、搜索、排行、Charts、对比、详情。每日摘要 = Pro。
2. 以后进 Pro、现在还没做的：Watch、日更/告警、更长历史、类型专属对比、导出。

### 线上不要出现的

3. 正式站不贴 Demo 标（Demo 只存在本地 `DATA_MODE=demo`）。
4. 首页四个搜索 chip：**不做**。
5. 详情页去掉安装命令 / Copy 展示，只留 GitHub 或网站链接。

### 代码 bug

6. **修 Skill 历史 join**：skill 的 `full_name` 是 `owner/repo / pdf`，`copyRepoMetricsToAssets` 却按仓名精确匹配，多 skill 仓没有 Star 曲线。应按 `source_repo_url` / `entity_key` 对齐仓库。共享的是**仓库级 Star**，不是 skill 自己的安装量。

### Skill 空榜（两条腿，校验继续严）

7. GitHub：放宽「去哪搜」，保留必须有 `SKILL.md`。Skill 本体几乎都在 GitHub。
8. Firecrawl（已配在线上）：爬 skills.sh 排行，写入名字、对应 GitHub、**安装量**。这是「用的人多不多」，不是自己再写爬虫。

### Website

9. 丢掉现在的独立源算法：`70% 信任 + 30% 页面新鲜度`。和「热度」无关。
10. 带 GitHub 的网站：跟仓库同一套，Star + 近窗口增量。详情官网优先、GitHub 其次。
11. 少数独立目录（skills.sh、Glama 自己）：有用量/安装量就按这个排；没有就不要写「变热 / 先看涨幅」。
12. Firecrawl 抽 Glama 的 usage/下载，补插件「多少人在用」。Website 列表页以后再扩，先把这 6 个独立源的**用量/关联仓热度**接上，不要采「页面更没更新」。

### 文案

13. Website 中英文案去掉「先看涨幅 / heating up」，改成跟仓库一致的热度语义，或等有增量数据再写涨。

---

## 以后再做（你点头的 P2）

- ~~Watchlist、名次变动、Since last visit~~（已做：Pro 关注列表）
- ~~Use-case 第二轴（Browser / Database…）~~（已做：榜单筛选 + 分类规则，不再用 topics[0]）
- ~~MCP / Skill 专属对比字段（仍不给安装命令，只给信息 + 源站链接）~~
- 语义搜索、API、评论、组件 Live preview、分类型 Trend Score
- ~~Official 从布尔改成证据；Category 不再用 `topics[0]`~~

---

## 明确不做

- 第七种资产类型
- 自己写爬虫替代 Firecrawl
- 用页面更新当排名
- Free 送每日摘要
- 详情页 Install/Copy 主按钮
- 用 catalog 假数据填 live 空榜
- 和 Glama 拼收录量

---

## 建议动手顺序

1. 修 Skill join + 对齐 PRODUCT.md  
2. 详情去掉 install 展示；Website 去掉信任/新鲜度计分和「涨幅」文案  
3. 放宽 GitHub Skill 搜索（仍要 SKILL.md）  
4. Firecrawl：skills.sh 安装量、Glama usage/下载，写入 `ranking_signals` 并在榜上展示
