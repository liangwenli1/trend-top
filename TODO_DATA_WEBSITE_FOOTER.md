# Trend Top：性能、数据覆盖、Website 与页脚待办

这份清单把当前发现的加载性能、采集覆盖、Website 数据来源和页脚改版放在一起。优先级按影响和依赖关系排列。

## P0：加载性能与可观测性

- [x] 排行的周期统计从逐条查询改为批量查询，避免条目越多请求越慢。
- [x] Charts 复用同一份前 50 名数据，不再分别计算前 5 名和前 50 名。
- [x] Charts 页面由一次接口同时返回图表和排名，去掉浏览器端的重复请求。
- [x] 全站搜索的六种类型并行查询；Compare 的多个详情并行查询。
- [x] 分类页复用排名样本；详情页、筛选项和首页统计中的独立查询并行执行。
- [x] 公共目录接口增加 60 秒浏览器缓存，并允许 5 分钟过期数据回退。登录、订阅、账单和后台接口继续禁用缓存。
- [ ] 在生产环境记录各接口的 P50、P95、数据库耗时、响应体大小和错误率；目标是缓存未命中时 P95 小于 800 ms，缓存命中时小于 150 ms。
- [ ] 为高频筛选建立与生产查询计划匹配的 PostgreSQL 索引，并用 `EXPLAIN (ANALYZE, BUFFERS)` 验证，不凭字段名盲目加索引。
- [ ] 当数据量超过 2 万条时，把非仓库榜单的筛选、排序和分页继续下推到数据库，避免每次把整类数据读入 Node.js 排序。

## P0：采集覆盖与漏收热门项目

当前采集不是随机抽样。语言和 Topic 根据 UTC 日期进行确定性轮换，每天覆盖其中一部分；同时每次采集有 2,000 个仓库和每类型 400 个候选的上限。

[`VoltAgent/awesome-design-md`](https://github.com/VoltAgent/awesome-design-md) 漏收有三个直接原因：主查询使用 `stars:50..80000`，超过 80,000 Star 的项目不在其中；之前没有设计系统类定向查询；类型查询按顺序填充时，前面的结果可能先占满 400 条上限。

- [x] 增加活跃的 80,000+ Star 仓库查询，避免热门项目只能依赖普通轮换碰到。
- [x] 增加 `design-system` 通用发现查询，以及 `awesome-design-md`、`awesome-list + design-system` 的 Website 定向发现。
- [x] 每个类型为每条定向查询保留候选预算，再用普通发现结果补齐，避免第一条查询占满全部名额。
- [ ] 把采集计划拆成固定配额：热门、近期创建、语言轮换、Topic 轮换、六大类型定向来源分别拥有最低名额；单组不能挤占其他组。
- [ ] 在 `sync_runs` 中记录每条查询的请求数、返回数、去重数、采纳数、限流与错误，后台展示“为什么收录/为什么没收录”。
- [ ] 增加 `last_seen_at`、`source_query` 和 `active` 字段。连续多次未发现的条目降权或隐藏，避免数据库只增不减导致首页“收录量”失真。
- [x] 调度器增加补采：02:00 UTC 的窗口错过后，根据数据库最后一次成功采集时间补跑一次；失败时每小时重试一次。
- [ ] 后台增加手动输入 GitHub 仓库或 Website URL 的候选入口，并记录审核状态；它用于补充采集规则，不能替代通用发现。
- [ ] Topic 继续完整保存用于筛选和搜索；列表默认只显示最相关的 3 个，展开最多显示 5 个。主题相关性应依据采集来源、名称/简介命中和全站稀缺度排序，不能直接使用 GitHub 返回顺序。

## P1：Website 成为独立数据类型

当前实时 Website 主要由 GitHub Repository Search 发现，并使用仓库 Star、Fork 和更新日期排序。它实际是“有 GitHub 仓库的网站项目”，不是独立的网站目录。`21st.dev` 等站点目前只存在于演示数据或组件条目中，没有独立的线上采集链路。

- [x] GitHub 返回有效 `homepage` 时，Website 的主按钮打开官网；没有官网时才回退到 GitHub。
- [ ] 数据模型分开保存 `website_url` 与 `source_repo_url`，详情页同时提供“访问网站”和“查看源码”，不要让官网地址覆盖来源证据。
- [ ] 建立 Website 来源注册表，首批覆盖 21st.dev、skills.sh、Smithery、Glama、Agent Skills、官方组件目录和独立工具站；每个来源保存抓取方式、更新频率和可信级别。
- [ ] Website 采集读取官网标题、描述、favicon、RSS/Atom、sitemap 和最近更新时间；遵循 robots.txt，并设置域名级限速和失败退避。
- [ ] Website 排名使用独立指标：近期内容更新、目录新增、公开使用量、社区讨论和关联仓库动量。GitHub Star 只能作为其中一个信号。
- [ ] 设计去重规则，把官网、GitHub 仓库、同名组件和同一产品的多个入口关联到一个实体，同时保留各自来源链接。
- [ ] 详情页清楚标记数据来源和最后抓取时间；缺少可靠指标时显示“数据积累中”，不生成虚假的增长值。

## P2：页脚改版

页脚右侧的 Home、Subscribe、Account、About 与顶部导航重复，占用了最显眼的区域。建议移除这组重复导航，把右侧改成一张轻量的“六类信号网络”视觉：六个方形节点分别代表 Skills、Plugins、Agents、Components、Websites、Repositories，细线缓慢连接，中间以蓝色节点表示 Trend Top。

- [ ] 使用 SVG 或 CSS 2.5D 视觉，不引入 Three.js；限制动画为 `transform` 和 `opacity`，避免拖慢页面。
- [ ] 鼠标移动时只做轻微视差和节点高亮；`prefers-reduced-motion` 下完全静止，移动端显示简化静态版本。
- [ ] 页脚底部保留 Pricing、Privacy、Terms、联系邮箱、X/Facebook/Telegram 和语言切换；这些链接不与顶部主导航重复。
- [ ] 管理后台未配置的社交链接不占位。Footer 文案继续强调六类开源发现，不再声称所有数据只来自 GitHub。
- [ ] 完成后检查首页、排行、Charts、详情和登录状态下的页脚高度、留白、暗色对比度与小屏布局。

## 发布验收

- [ ] 连续执行两次采集，确认 `awesome-design-md` 被收录为 Website 和 Repository，Website 主链接打开有效官网或明确回退 GitHub。
- [ ] 对六大类型逐一打开 Trending、Rankings、Charts、详情和 Compare，确认没有重复请求、长时间空白或串行等待。
- [ ] 验证筛选、Topic/Language 点击、分页、登录、订阅、Creem 结账和后台设置不受公共缓存影响。
- [ ] 在桌面端和移动端验证页脚动效不会引起横向滚动、布局跳动或明显 CPU 占用。
